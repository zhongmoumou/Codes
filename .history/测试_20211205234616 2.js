/* ### 核心算法：

先查找第一个 o 出现的位置；

然后只要 indexOf() 返回的不是 -1，则继续向后查找；

因为 indexOf() 只能查找到第一个，所以继续向后查找需要利用第二个参数（当前索引加 1），从而继续查找 */

let str = 'abcoefoxyozzopp';

let oneO = indexOf('o');

while(true) {
  if () {
    
  }

}